version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd web
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: web/.next
    files:
      - "**/*"
  cache:
    paths:
      - web/node_modules/**/*
